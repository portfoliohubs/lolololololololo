# ملاحظات الترحيل وتوصيات التشغيل المستقبلي
# MIGRATION NOTES & OPERATIONAL PLAYBOOK

**المنظومة:** Parmaga LMS Engine  
**تاريخ الترحيل:** سبتمبر 2026  
**الإصدار بعد الترحيل:** v2.3 Hardened & Normalized  

---

## 1. التغييرات الجوهرية المنفذة (Key Architectural Shifts)

1. **التحول من التخزين العام إلى التخزين المحمي (Protected Storage):**
   * المسار القديم: `public/content/...` (ملغى تماماً للأسئلة والمحتوى الحساس).
   * المسار الجديد: `protected_content/...` (خارج النطاق العام، مخدوم عبر حراسة الخادم `server.ts`).

2. **تحويل نوع معرفات الوحدات في Firestore:**
   * الحقل المستهدف: `users.unlockedUnits`.
   * النوع السابق: خليط غير متناسق من النصوص (`"unit-1"`, `"unit_1"`).
   * النوع الجديد: **مصفوفة أرقام صحيحة صارمة `number[]`** مثل `[1, 2, 3]`.
   * **ملاحظة ترحيل:** في حال وجود حسابات قديمة مسجلة بسلاسل نصية، تم تزويد `AuthContext.tsx` ومحرك السيرفر `server.ts` بفلتر ذكي يحول أي نص `"unit-X"` أو `"unit_X"` إلى الرقم المقابل `X` تلقائياً لتفادي انقطاع الخدمة عن أي طالب سابق.

3. **طبقة خدمة العميل (`contentService.ts`):**
   * تم استبدال جميع استدعاءات `fetch('/content/...')` في المكونات (`CurriculumStations.tsx`, `LessonViewer.tsx`, `ExamEngine.tsx`, `FinalExamsHub.tsx`, `FlashcardsEngine.tsx`) باستدعاءات موحدة عبر `contentService`.
   * هذه الخدمة تقوم تلقائياً بإرفاق `Authorization: Bearer <Firebase_ID_Token>` مع كل طلب وتتعامل مع استجابات `401` و `403` بسلاسة عبر عرض رسائل توجيهية للطالب للاشتراك بدلاً من انهيار الواجهة.

4. **طبقة أوامر الإدارة الموثقة (`adminService.ts`):**
   * عند قيام المشرف أو المساعد بأي إجراء (فك ارتباط جهاز، تفعيل وحدة، تغيير باقة، تجميد حساب):
     1. يتم إرسال الطلب إلى السيرفر المحمي أو تنفيذ معاملة بنكية `runTransaction` في Firestore.
     2. يتم كتابة سجل غير قابل للتعديل في `audit_logs` يتضمن المنفذ، المستهدف، القيم السابقة، القيم الجديدة، والسبب.
     3. تقوم الواجهة فوراً باستدعاء `fetchStudents()` لقراءة البيانات الحقيقية المؤكدة من السيرفر قبل إغلاق النافذة.

---

## 2. إجراءات ما بعد النشر (Post-Deployment Checklist)

عند نشر المنصة في بيئة الإنتاج السحابية (Firebase Hosting / Cloud Run):
1. **نشر قواعد الأمان:**
   ```bash
   firebase deploy --only firestore:rules
   ```
2. **التحقق من ملفات البيئة (`.env`):**
   * التأكد من تعريف متغيرات Firebase في البيئة السحابية:
     * `VITE_FIREBASE_API_KEY`
     * `VITE_FIREBASE_AUTH_DOMAIN`
     * `VITE_FIREBASE_PROJECT_ID`
     * `VITE_FIREBASE_STORAGE_BUCKET`
     * `VITE_FIREBASE_MESSAGING_SENDER_ID`
     * `VITE_FIREBASE_APP_ID`
3. **التشغيل والتحقق التلقائي المستمر:**
   * تشغيل الاختبارات الآلية قبل أي دمج مستقبلي:
     ```bash
     npm test
     ```
   * التأكد من ظهور 14 اختباراً ناجحاً بنسبة 100%.

---

## 3. التعامل مع الاشتراكات والدفع الإلكتروني

* عند ربط بوابات الدفع الإلكتروني (فودافون كاش، إنستاباي، فوري، كروت الائتمان):
  * يتم استلام الـ Webhook الخاص بنجاح العملية على مسار مخصص في السيرفر.
  * يقوم السيرفر باستدعاء منطق مشابه لـ `adminService.setStudentAccess` لتحديث حالة الطالب إلى `active`، وضبط `packageId`، وتاريخ الانتهاء `subscriptionExpiresAt`، والوحدات المفتوحة `unlockedUnits`، مع توثيق العملية في `audit_logs`.
